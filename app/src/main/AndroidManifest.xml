<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pruebacamara.proyecto.analisis.analisiscamara">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
            Se agrega un provideer para el correcto funcionamiento de los URI en la aplicación, esto
            ya que presentan errores en versiones vayores de android, se indica un nombre y un auto-
            rizador obligatorio, el authorities debe coincir con todos los providers que se pasen
            por parámetro al método constructor del URI, sino croma.
            El metadata es necesario, se le indica donde se encuentra el xml con paths generalmente
            hay que crearlo, android studio no lo coincidera necesario entonces no es creado.
        -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="pruebacamara.proyecto.analisis.analisiscamara.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths">
            </meta-data>
        </provider>

    </application>

</manifest>